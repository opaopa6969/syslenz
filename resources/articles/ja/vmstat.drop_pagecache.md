# drop_pagecache

[English version](../en/vmstat.drop_pagecache.md)

---

`drop_pagecache` — ページキャッシュ削除イベント（drop_cachesに1/3を書き込み）。

**ソース:** `/proc/vmstat`  
**単位:** 起動からの累積カウント（単調増加）

---

## 関連項目

- `sourceguide.vmstat` — vmstatソース全体の概要
- `pressure.memory_some_avg10` — カーネルのメモリストールシグナル
