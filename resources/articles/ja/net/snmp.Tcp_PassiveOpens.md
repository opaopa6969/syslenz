# Tcp_PassiveOpens

[English version](../../en/net/snmp.Tcp_PassiveOpens.md)

---

**[net/snmp.Tcp](../Tcp.md)** グループの一部です。

`Tcp_PassiveOpens` — パッシブにオープンされたTCP接続（listen/acceptによる）。

**ソース:** `/proc/net/snmp`  
**単位:** 累積カウント

---

## 関連項目

- `net/snmp.Tcp` — プロトコルグループ記事
- `sourceguide.net/snmp` — ソース全体の概要
