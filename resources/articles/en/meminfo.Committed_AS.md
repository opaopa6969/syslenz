# Committed_AS

[日本語版](../ja/meminfo.Committed_AS.md)

---

`Committed_AS` — Total memory committed by all processes (may exceed physical RAM).

**Source:** `/proc/meminfo`  
**Unit:** kB

---

## See also

- `meminfo.MemAvailable` — overall memory availability
- `sourceguide.meminfo` — full meminfo source overview
