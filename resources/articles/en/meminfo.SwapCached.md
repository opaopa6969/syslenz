# SwapCached

[日本語版](../ja/meminfo.SwapCached.md)

---

`SwapCached` — Swap pages also present in RAM (avoids re-reading from swap on reuse).

**Source:** `/proc/meminfo`  
**Unit:** kB

---

## See also

- `meminfo.MemAvailable` — overall memory availability
- `sourceguide.meminfo` — full meminfo source overview
